# connected_components_B-W
Connected components algorithm in python with maximum number of components is "SIX". It fills the different components with different colors. It is specific for black and white image and choose only those components whose size is greater than 650 pixels.
